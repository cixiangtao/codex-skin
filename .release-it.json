{
  "$schema": "https://unpkg.com/release-it@20/schema/release-it.json",
  "git": {
    "commit": true,
    "commitMessage": "chore(release): v${version}",
    "requireBranch": "release/npm-v*",
    "tag": false,
    "push": false,
    "requireCleanWorkingDir": true
  },
  "github": {
    "release": false
  },
  "gitlab": {
    "release": false
  },
  "hooks": {
    "before:init": "bun run release:check",
    "after:bump": "bun install --lockfile-only && test \"$(git branch --show-current)\" = \"release/npm-v${version}\"",
    "after:release": "echo Prepared release PR commit for ${name} v${version}"
  },
  "npm": {
    "publish": false
  }
}
